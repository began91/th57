const generateURL = state => {
    //assemble pieces of state to generate the current url

    //possibly set the current url???
}




const parseURL = () => {
    const urlParams = new URLSearchParams(window.location.search);
}
